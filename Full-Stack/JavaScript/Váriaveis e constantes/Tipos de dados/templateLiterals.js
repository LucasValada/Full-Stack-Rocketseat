// Template literals (template strings, interpolação de strings)

const nome = "Lucas";
const email = "lucas@email.com";
const age = 23;
const message = `O meu nome é ${nome} e eu tenho ${age} anos. Meu email é ${email}`;
console.log(message);
