// O meotodo some() testa se pelo menos um dos elementos do array passa no teste implementado pela funcao fornecida.
console.log("### METODOS DE ARRAY || SOME ###");
const agesSome = [15, 19, 20, 25, 30];
const resultAgeSome = agesSome.some((age) => age < 18);
console.log(resultAgeSome);
